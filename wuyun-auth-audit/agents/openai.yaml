interface:
  display_name: "Wuyun Auth Audit"
  short_description: "JWT, OAuth, SAML, session, and tenant authz review"
  default_prompt: "Use $wuyun-auth-audit with $wuyun to inventory JWT/OAuth/SAML/session/cookie/tenant authorization surfaces, generate safe auth hypotheses, and plan owned-account validation without brute force or credential theft."
